export const metadata = {
  title: "  Generate  | S.LinkShortener",
  description: "Manage your expenses easily",
};

export default function GenerateLayout({ children }) {
  return <>{children}</>;
}
